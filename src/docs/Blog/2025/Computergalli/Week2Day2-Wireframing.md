<ol> - tag for an ordered list of items (numbered list)
<ul> - tag used to create an unordered list of items
<li> - tag used to represent an item in a list
<table> - makes a table od data 
<thead> - element encapsulates a set of table rows , head of the table with info about the table's columns 

<tbody> the tag used to create an element that encapsulates a set of table rows 
<th>
